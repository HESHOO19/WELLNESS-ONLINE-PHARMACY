# Wellness Pharmacy Environment Configuration
# Copy this file to .env and configure your settings

# Server
NODE_ENV=development
PORT=3000

# PostgreSQL Database Connection
# Option 1: Individual settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wellness_pharmacy
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_SSL=false
DB_POOL_SIZE=10

# Option 2: Connection URL (overrides individual settings if set)
# DATABASE_URL=postgresql://user:password@host:5432/wellness_pharmacy?sslmode=require

# For cloud providers like Supabase, Neon, Railway, etc:
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
